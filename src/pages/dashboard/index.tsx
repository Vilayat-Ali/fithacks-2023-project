// lib
import PageWrapper from "@/components/PageWrapper";

type Props = {};

const Dashboard = (props: Props) => {
  return (
    <PageWrapper
      title="Profile"
      description=""
      keywords={[]}
      isProtected={true}
    >
      <div>Dashboard</div>
    </PageWrapper>
  );
};

export default Dashboard;
