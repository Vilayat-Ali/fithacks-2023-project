// components
import PageWrapper from "@/components/PageWrapper";

type Props = {};

const Profile = (props: Props) => {
  return (
    <PageWrapper
      title="Profile"
      description=""
      keywords={[]}
      isProtected={true}
    >
      <div>Profile</div>
    </PageWrapper>
  );
};

export default Profile;
